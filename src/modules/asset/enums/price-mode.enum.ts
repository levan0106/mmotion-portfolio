/**
 * Price Mode Enum
 * Defines how asset prices are managed
 */
export enum PriceMode {
  AUTOMATIC = 'AUTOMATIC', // Auto-sync from market data
  MANUAL = 'MANUAL'        // Manual price entry only
}
